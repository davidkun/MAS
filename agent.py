#!/usr/bin/env python

class agent():
  """ Represent an agent in
  a multi-vehicle system (MAS).
  
  The agent is dynamic, reads location transmissions,
  and has the ability to communicate its own state."""

